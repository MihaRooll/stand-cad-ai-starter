# Deferred verification ledger

Running ledger of verification debt accumulated during **FAST ITERATION MODE** (D-043). Each deferred check must be discharged before the post-approval verification campaign can be declared complete. Deferred items are debt, not waivers — append one row per deferred item; set `Status` to `CLOSED` only when the check has been executed and evidence recorded.

| Date | Change / decision ref | Deferred check | Why deferred | Status |
|---|---|---|---|---|
| 2026-08-05 | D-043 | Full profile + adversarial review of every fast-iteration edit | FAST ITERATION MODE — owner prioritizes rapid geometry/appearance review in live viewer over exhaustive verification per edit cycle | OPEN — PROD-001 S-4+ adversarial passes pending |
| 2026-08-05 | D-044 | Structural analysis of top perimeter ring resistance to front-top side-wall splay with the FRONT leg removed | FAST ITERATION MODE — owner-mandated visual/geometry change; no FEA/hand-calc capacity in this cycle; ring is now closed on 3 of 4 sides only | OPEN |
| 2026-08-05 | D-045 | Re-run mass/stability/deflection reports at new `case.depth`=420 mm | FAST ITERATION MODE — deferred until owner visual 3D approval per D-043 | CLOSED |
| 2026-08-05 | D-046 | Physical film-glide/friction validation of `MEDIA-SUPPORT-L{1,2}-001` surfaces and the 10 mm (below the prior 12 mm TZ floor) exit aperture on real film stock | FAST ITERATION MODE — geometric clearance confirmed by a deterministic sweep test only; no physical prototype test in this cycle | OPEN |
| 2026-08-05 | D-047 | Physical fit-check of the relocated Ø30 cable grommet against the real USB service-port connector once both are selected/measured (both currently `to_measure` diameter/wall/connector) | FAST ITERATION MODE — geometric clearance confirmed by targeted tests only; no physical prototype test in this cycle | OPEN |
| 2026-08-05 | D-048 / D-049 | Qualified engineering review of indicative tip-over factors (PLT-010 split stationary/moving static-moment model) for Gate G4 — FEA, dynamic transport loads, measured masses | Indicative pytest gate passes at 250 mm extension / 420 mm depth (D-049: upper 2.339, lower 2.650); model remains concept-stage only and was never authoritative for G4 | OPEN |
| 2026-08-05 | D-050 / D-051 | Physical carry ergonomics — balance-point grip Y=165.7 mm (loaded CoM) vs geometric depth centre Y=210 mm; grip Z=252 mm vs loaded CoM z≈227.5 mm; **OPEN handle-concept blocker:** through-cutout grip intersects tier-2 plotter bay (~1.78×10⁶ mm³ finger-reach overlap at Y=165.7 / Z=252 — not usable as modelled); owner deferred choosing external bolt-on handle, blind side pocket, or aft low-mounted cutout; whether one person can lift ~8.5 kg without knee strike or front-heavy tip | FAST ITERATION MODE — geometric/sightline clearance confirmed by targeted tests only; owner must validate on prototype and resolve handle concept before production | OPEN |
| 2026-08-05 | D-052 | Reportlab PDF renderer has no automated visual/pixel regression test — only structural `SheetSpec` assertions in `tests/test_drawings.py` | Drawing package stage — correctness of layout/markings verified via pure data model; pixel-level PDF review deferred | OPEN |
| 2026-08-05 | D-052 | DXF outline extraction picks the single largest face per part and does not verify it is the intended front face for every part shape | Prototyped method confirmed clean extraction with no non-planar warnings on current flat-panel parts; per-part face-intent audit not automated | OPEN |
| 2026-08-05 | D-054 (fix wave) | Owner ruling on lid-headroom conflict — 27 mm clear over tier 1 / 50 mm over tier 2 against an 80 mm provisional open-lid envelope (`plotter.lid_open_envelope_height_mm`, `to_measure`) | Real hinge axis/swing radius/opening direction not measured (§A items 3–4); `tests/test_kinematics.py::test_lid_envelope_no_intersection_in_service_states` marked **`@pytest.mark.xfail(strict=True)`** with transport headroom canary — do not weaken to green pytest; pending owner decision on redesign (relocate lid pivot, cut headroom elsewhere, or accept lid-open-only-when-extended as an operating constraint) | OPEN |
| 2026-08-05 | D-054 (fix wave) | Owner decision + hardware selection for transport retention — no restraint modeled for trays (soft stops travel with the tray, contact no fixed structure), plotters (elastomer pads only, no tie-down), or film (front retainer `RETAINER-001` removed by the D-031 horizontal-organizer replacement); unrestrained mass 4.7 kg + 5.2 kg plotters plus up to 10 kg film, against TZ lines 158/181/303 | FAST ITERATION MODE / PLT-007 horizontal-reconfig scope — transport-lock hardware (detents, tie-downs, removable retainer) is new mechanical design, not a defect fixable in this geometry cycle | OPEN |
| 2026-08-05 | D-054 (fix wave) | Reconcile side-slab mass model before citing headline mass figures — `PANEL-OUT-LEFT/RIGHT-001` use the `cast_opal_pmma_3mm` shell-thickness mass path while the actual modeled geometry is a solid 20 mm slab (≈4.37×10⁶ mm³ ≈5.15 kg each by volume × density); shell model reports ≈0.395 kg each, understating each slab by ≈4.75 kg (≈9.5 kg aggregate across both slabs) — current headline **structural ≈6.048 kg / all-parts ≈8.479 kg** are not settled pending this reconciliation (PLT-012 / `scripts/generate_drawings.py` `OPEN-001` sheet already surfaces the per-slab numbers programmatically in every generated drawing package) | Requires an owner/engineering decision on which mass-accounting policy is authoritative for a genuinely-solid slab (correct the density path vs. document the slab as an intentionally-simplified shell surrogate) — not a code defect to silently pick a side on | **CLOSED — D-055** cavity-wall geometry + `/2` shell-formula fix; see `docs/10_USER_INPUT_REQUIRED.md` §O |
| 2026-08-05 | D-055 | Physical validation of side-slab cavity-wall return-flange choice and illuminated-side appearance vs reference photo | CONCEPT-stage geometry models 3 mm outer skin + front/rear returns; real fabricated panel may differ in flange detail / bonding | Prototype visual + backlight test | Gate G2 appearance review | OPEN |
| 2026-08-06 | Corner post top raised to `case.height` (owner-flagged visual defect: bare unsupported acrylic lip/nub above the 4 frame corner posts, `top_structure` band 529–544 mm was frame-less) | — | **SUPERSEDED — D-058**: this approach (extend post to fill the band) was rejected after two more iterations; root cause fixed instead by removing the band itself (`case.height` 544→529) | CLOSED (superseded, not verified) |
| 2026-08-06 | Exposed corner-post top band (`top_structure` 529–544 mm) reshaped to a thin single-square stub per corner instead of reusing the full L-shaped bridging cross-section — the new taller post from the row above made the existing structural L-leg visible above the rail line, reading as a lopsided sideways tab in a top-down view | — | **SUPERSEDED — D-058**: still read as an unwanted "staple" silhouette; the cap logic (`_corner_post_cap_solid`) is now a permanent no-op since `case.height`=529 removes the band it was capping | CLOSED (superseded, not verified) |
| 2026-08-06 | D-059: `LIGHT-STRIP-001` repositioned under `FRAME-RAIL-TOP-REAR-001` instead of above the (now-removed) `top_structure` headroom band | Confirm the new position doesn't put the LED strip somewhere unreachable/unmountable in the real rear-top cavity once that construction is finalized | FAST ITERATION MODE, no tests run; verified only by `minimum_clearance()`/`intersection_volume()`=0 against neighbours | **CLOSED — D-062** `tests/test_geometry.py::test_light_strip_below_roofline_d059`; assembly sequence Step 7 flags service access before rear panel |
| 2026-08-06 | D-059: new `SHELF-SUPPORT-{L,R}-{000,001,002}` parts (mechanical cleat placeholder closing the 17 mm shelf-to-wall gap) | Cleat attachment method **CLOSED — D-065**: 3×M4 pan-head into M4 rivnuts in Al cleat (adhesive-free); hole positions / grip to_measure on prototype; collision re-check with fasteners at prototype | Owner chose quick placeholder ledge 2026-08-06; **JT-SHELF-SUPPORT-SKIN decided D-065**; geometric bearing **CLOSED** via `test_shelf_supports_bear_on_side_panels` | **CLOSED (D-065)** — prototype hole/grip verification remains OPEN |
| 2026-08-06 | D-059: rear vertical case edge (`PANEL-OUT-LEFT/RIGHT-001`) now filleted to match the front's R10-class bullnose (`_apply_top_rear_bullnose`) | Full `pytest` re-run (existing side-slab geometry tests assume/measure the front-only fillet and may need a symmetric-edge assertion added); mass model delta from the small material removal at the rear corner not re-quantified; no check yet on whether other rear-adjacent parts (rear panel, rear corner posts, cladding) need a matching safety edge break | Owner safety request 2026-08-06 ("острые углы... надо скруглить"); fast-iteration, no tests run; confirmed geometrically via vertex inspection (tangent point at X≈9.9 mm, Z=9 and Z≈519) | **CLOSED — D-062** `tests/test_geometry.py::test_side_slab_rear_vertical_bullnose` |
| 2026-08-06 | `case.height` 544→529 mm (`top_structure.height_mm` 15→0) — removes an unused 15 mm allowance no part ever occupied, fixing the corner-nub issue at its root (D-058) | Full `pytest`/`ruff`/`scripts/regenerate.py`/mass+stability+deflection report refresh; every test and doc that cites case height as 544 mm (e.g. any hardcoded "650 × 420 × 544" bounding-box assertion, ADR-005 envelope text, ASSUMPTIONS/traceability rows) needs a pass to retarget 529 mm; re-confirm D-038's independent +27 mm term is untouched | FAST ITERATION MODE — owner explicitly asked for a quick visual-only fix after 3 iterations, no tests run; verified only by direct bounding-box measurement + 2 orthographic renders (not committed to `output/validation/`) | **CLOSED — D-062** `test_case_height_529_after_d058`; rev13 regenerate; D-038 `upper_z`/tray stack untouched |
| 2026-08-06 | Mirrored the D-037 FL-only film-withdrawal clearance notch onto `FRAME-POST-FR-001` / `PANEL-CLAD-FRONT-POST-FR-001` for **visual symmetry only** — confirmed by measurement that the right side has no functional need for it (film sheets are left-aligned in the storage bay, 110 mm short of the right post per `test_film_bodies_span_sheet_depth_across_width`); owner chose the cosmetic-mirror option over leaving the (correct, functionally-justified) asymmetry | This is a deliberate **owner-selected cosmetic deviation from the D-037 rationale**, not a defect fix — record if a future reviewer flags "why does FR have an unused clearance notch," and re-confirm no new collision/mass-model side effect once full verification resumes | Owner decision 2026-08-06 (chose "cosmetic_mirror" over "keep_honest asymmetry" / "re-centre sheets"); `src/stand_cad/geometry/frame.py` (`FRAME-POST-FR-001` branch, `PANEL-CLAD-FRONT-POST-FR-001` `suffix == "FR"` branch) | **CLOSED — D-062** `test_fr_post_film_notch_mirror` (cosmetic rationale remains documented) |
